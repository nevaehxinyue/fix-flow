import React from "react";
import DrawerButton from "./DrawerButton";
import { Box } from "@radix-ui/themes";

const TopBackgroundBox = () => {
  return (
    <Box className="absolute bg-gradient-bronze shadow-lg border-0  inset-0 z-0 h-60 ">
     
    </Box>
  );
};

export default TopBackgroundBox;
